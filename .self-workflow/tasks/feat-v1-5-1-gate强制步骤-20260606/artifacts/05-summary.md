# 05-summary：V1.5.1 Gate强制步骤 — 总结沉淀

> **workflow-id**: feat-v1-5-1-gate强制步骤-20260606
> **时间戳**: 2026-06-06T20:40:00+08:00

## 本次得与失

### 得（做得好的）

1. **自举验证成功**：本次工作流本身即运行在 feat-workflow.md v0.2 下，通过 5 个阶段完整验证了要修复的 3 个问题——其中 F3（Gate 量化）在每个 Gate 入口都被手动计算，F2（ADR 文件）被强制遵守（4 个 ADR 文件均已创建），F1（Git tag）每个 Gate 都创建了 checkpoint。

2. **Human 反馈即时吸收**：Human 发现 "ADR 重复在 02-design.md 和 adrs/ 目录"，立即创建 ADR-004 归档此决策。这证明了 F2 "决策即时归档"机制的合理性。

3. **Review Agent 审查形成闭环**：Gate 1 审查发现 3 条 info，其中关于"决策声明绕过"的问题直接驱动了 ADR-003 的产生。Gate 2 方向审查的 2 个 warning（量化输出通道、Review Agent 同步）都在实施前吸收。

### 失（需改进的）

1. **审查等待时间**：2 次 Review Agent 调用（Gate 1 和 Gate 2）各耗时 ~1.5 分钟，占工作流总时间的相当比例。这是 Review Agent 审查深度的合理代价。

2. **文档规模增长**：feat-workflow.md 从 600 行增长到 ~660 行（+10%）。每次迭代新增约束都会使文档更复杂。V2 需考虑模块化拆分。

## 决策清单

| ADR | 决策 | 备注 |
|-----|------|------|
| 001 | F3量化公式混合方案 | 附录 canonical + Gate入口自包含 |
| 002 | Compound补建 git log --grep | 含 fallback 时间戳搜索 |
| 003 | ADR检查显式声明防绕过 | Review Agent 扩展入后续计划 |
| 004 | 设计文档ADR引用而非内联 | Human 反馈驱动的即时归档 |

## doc 级经验

本次任务产出了可跨任务复用的经验——写入 `.self-workflow/docs/`：

### `${workflow-id}-实施经验.md`

**核心发现**：Gate 强制步骤的有效性取决于 3 个维度：
1. **措辞**：从 "是否需要？" 改为 "检查...目录——必须有文件"（F2 的教训）
2. **位置**：将量化公式从"共享引用"改为"入口自包含"（F3 的教训）
3. **兜底**：事后补建 > 事前阻止（F1 的教训——Agent 跳过 tag 创建时 Compound 兜底）

**可推广模式**：当 Agent 反复跳过某规范步骤时，三管齐下——
1. 措辞改为存在性断言（而非提示性问题）
2. 将关键内容从引用处复制到执行点（Agent 不会跳转读取）
3. 在 Compound 阶段增加事后修复逻辑
