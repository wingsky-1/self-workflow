# 合并 + 引用完整执行方案

> 原始会话分析文档归档。包含 3 组合并的完整文档内容模板和 10 组引用的精确文本。
> 此方案在会话中逐对分析后产出，直接驱动 Phase 3 执行。

---

## 合并 M1：安装器认知陷阱（2→1）

**合并**：`错误经验/installer-错误经验.md` + `错误经验/design-可定制性声明验证-错误经验.md`
**新文件**：`错误经验/安装器三层架构与自举认知陷阱.md`

**决策理由**：同一根因（三层架构认知缺失）的两个表现面——执行面+设计面。B 文档自述为 A 的延伸。

**完整文档内容**：见 `错误经验/安装器三层架构与自举认知陷阱.md`（已创建）

---

## 合并 M2：ADR 推广（2→1）

**合并**：`参考模式/产物权威来源唯一-ADR引用而非内联.md` + `参考模式/推广ADR-运行时文档与任务记录分离.md`
**新文件**：`参考模式/ADR推广——位置与写法.md`

**决策理由**：Agent 晋升 ADR 时需同时回答"放哪里"和"怎么写"。两份互补不重叠。

**完整文档内容**：见 `参考模式/ADR推广——位置与写法.md`（已创建）

**内含引用**（R2 + R3）：
- → `跨任务决策沉淀与 ADR 治理`：本文讲 HOW，该文档讲 WHEN
- → `ADR 与设计文档交叉一致性审查`：本文定义规则，该文档定义审查

---

## 合并 M3：提取/删除防偏差（2→1）

**合并**：`参考模式/验收验证的双重检查-存在与不存在.md` + `错误经验/委托提示词歧义-提取不等于保留副本.md`
**新文件**：`参考模式/提取删除类变更——防执行偏差.md`

**决策理由**：同一 V1.9 事件（模板提取）的根因+方案。分离造成诊断链路断裂。

**完整文档内容**：

```markdown
---
title: "提取/删除类变更——防执行偏差"
category: 参考模式
tags: [extraction, verification, negative-check, delegation, prompt-design]
date: 2026-06-06
source: tasks/feat-v1-9版本-20260606
quality: verified
---

# 提取/删除类变更——防执行偏差

## 案例

V1.9 将 task.yaml 模板从 `feat.md` 内联提取为独立文件 `feat-task.yaml`。

**预期结果**：1 份外部模板 + 1 行引用指令

**实际结果**：1 份外部模板 + 68 行内联副本——完全没有实现"提取"意图。

## 根因：委托提示词歧义

Prompt 中写了"保留 YAML 块作为 fallback 注释"——意图是"保留引用注释"，deep agent 理解为"保留完整 68 行 YAML 作为内联 fallback"。

| 层面 | 问题 |
|------|------|
| 语义歧义 | "保留 YAML 块"被理解为保留内容而非保留引用 |
| 操作描述不清 | prompt 只明确了 extract（创建文件），未明确 replace（删除内联） |
| 验证缺失 | 只检查"目标文件是否存在"，未检查"源文件内联是否已移除" |

## 方案一：防歧义的 Prompt 写法

对于 extract + replace 操作，用 **delete-then-create** 的否定式描述：

```
MUST DO:
- DELETE 行 X 到行 Y 的全部内容（包括 ```yaml 和 ``` 标记）
- 在删除位置写入"从 <path> 加载模板"

MUST NOT DO:
- 不得以"fallback""备份""注释内保留"等任何形式保留被删除内容
```

❌ 歧义写法：`保留 YAML 块作为 fallback 注释`
✅ 精确写法：`将内联 YAML 块完整删除。在删除位置写入引用指令。不得保留在任何形式中。`

## 方案二：双重验证

对替换/删除类变更，每条验收标准写为**一对检查**：

```markdown
- [ ] A2a：正向——新文件存在且内容正确
- [ ] A2b：反向——旧位置不存在被提取的内容
```

**通用自检**（标记验收标准为 PASS 前）：

> 这条变更是否涉及"删除"或"替换"旧内容？
> 如果是 → 我是否同时检查了"新内容存在"**和**"旧内容不存在"？

## 验证清单

- [ ] 新文件存在且内容正确（正向）
- [ ] 旧位置不存在被提取的内容——搜索关键字段确认已删除（反向）
- [ ] 引用路径与 MANIFEST 部署路径一致

## 关联经验

- **`Phase Gate 验证不能形式化`**（`错误经验/`）—— 与本文的差异：本文讲"正确的验证怎么做"（正向+反向双重检查），该文档讲"什么是不对的验证"（只做文件存在性检查的形式化验证）。两篇合在一起形成完整的验证方法论。
```

---

## 引用 R1：跨任务决策沉淀 → ADR推广

**方向**：单向
**添加到**：`关键决策/跨任务决策沉淀.md` 末尾

```markdown
## 关联经验

- **`ADR 推广——位置与写法`**（`参考模式/`）—— 与本文的差异：本文定义 ADR 的晋升标准和触发时机（WHEN），该文档定义晋升时的具体操作——完整内容放哪里、推广版怎么写（HOW）。晋升流程的 Phase 5 步骤必须两篇同读。
```

---

## 引用 R4：ADR与设计文档交叉一致性审查 → ADR推广

**方向**：单向（R3 的反向已在 M2 文档内部）
**添加到**：`参考模式/ADR与设计文档交叉一致性审查.md` 末尾

```markdown
## 关联经验

- **`ADR 推广——位置与写法`**（`参考模式/`）—— 与本文的差异：本文是审查流程（如何发现 ADR 与产物不一致），该文档是被审查的规则本身（ADR 应放在哪里、推广版怎么写）。审查前必须先读规则。
```

---

## 引用 R5：phase-gate-验证不能形式化 → 提取删除防偏差

**方向**：单向（R6 反向已在 M3 文档内部）
**添加到**：`错误经验/phase-gate-验证不能形式化.md` 末尾

```markdown
## 关联经验

- **`提取/删除类变更——防执行偏差`**（`参考模式/`）—— 与本文的差异：本文讲验证不能形式化（反面警告——仅文件存在性检查不够），该文档讲正确的验证模式（正面方案——正向检查+反向检查+防歧义 prompt）。两篇合在一起形成完整的验证方法论。
```

---

## 引用 R7：gate-强制步骤 ⇄ gate-推理链一致性（双向）

**添加到**：`实施经验/gate-强制步骤-实施经验.md` 末尾

```markdown
## 关联经验

- **`Gate 推理链一致性`**（`错误经验/`）—— 与本文的差异：本文解决 Agent **跳过** Gate 步骤的问题（三管齐下——措辞/位置/兜底），该文档解决 Agent **执行了** Gate 但忘记前置约束的问题（Phase 2 推翻 Phase 1 须显式说明）。两个问题共享"Gate 纪律"主题但触发场景不同——排查 Gate 违规时两篇都应检索。
```

**添加到**：`错误经验/gate-推理链一致性-错误经验.md` 末尾

```markdown
## 关联经验

- **`Gate 强制步骤实施经验`**（`实施经验/`）—— 与本文的差异：本文解决跨阶段推理链断裂，该文档解决 Agent 完全跳过 Gate 步骤。如果 Agent 连 Gate 本身都跳过了，本文的"反转说明模板"无从谈起——该文档的三管齐下模式是前置防线。
```

---

## 引用 R8：readme-作为配置权威源 → spec上下文注入架构

**单向**。添加到：`参考模式/readme-作为配置权威源.md` 末尾

```markdown
## 关联经验

- **`Spec 上下文注入架构`**（`关键决策/`）—— 与本文的差异：本文是内容组织模式——如何把结构化数据嵌入人类可读文档（README.md 底部分类定义段）；该文档是运行时机制——Plugin 如何解析这些数据并注入 Agent 上下文（双钩子：system.transform + tool.execute.before）。内容侧 + 机制侧 = 完整方案。
```

---

## 引用 R9：plugin-session-inject-pattern → spec上下文注入架构

**单向**。添加到：`参考模式/plugin-session-inject-pattern.md` 末尾

```markdown
## 关联经验

- **`Spec 上下文注入架构`**（`关键决策/`）—— 与本文的差异：本文是参考实现——具体的 TypeScript 代码（marker 检测 + 合并而非 push）；该文档是设计决策——为什么选双钩子架构、为什么 marker 优于内存 Set。实现前先读设计决策理解"为什么"，再参考本文看"怎么写"。
```

---

## 引用 R10：spec文档写法 ⇄ LLM指令设计（双向）

**添加到**：`参考模式/spec文档写法-规范而非步骤.md` 末尾

```markdown
## 关联经验

- **`LLM 指令设计原则——语义理解优先于机械规则`**（`参考模式/`）—— 与本文的差异：本文讲格式层面怎么写规范（MUST/SHOULD/MAY 分级、规则而非步骤），该文档讲语义层面怎么写才能让 LLM 真正理解（语义优先于机械规则）。两篇合在一起形成"写给 LLM 的内容设计"完整指南——格式层（本文）+ 语义层（该文档）。
```

**添加到**：`参考模式/LLM指令设计-语义优先于机械规则.md` 末尾

```markdown
## 关联经验

- **`Spec 文档写法——规范而非步骤`**（`参考模式/`）—— 与本文的差异：本文讲语义层面的指令设计（LLM 理解优先于机械替换），该文档讲格式层面的规范写法（MUST/SHOULD/MAY、规则而非步骤脚本）。该文档是本文"语义优先"原则在 spec 领域的具体落地。
```

---

## 执行统计

| 操作 | 数量 |
|------|------|
| 合并写入新文档 | 3 |
| 删除旧文档 | 6 |
| 添加引用节 | 10 次编辑（跨 10 份文档；M2 内含 2 组引用在合并文档内） |
| 净变化 | 24 → 21 份文档 |
